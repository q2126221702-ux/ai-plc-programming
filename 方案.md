这是你现在整个系统里非常关键的一个工程问题，而且：

# 这是所有“LLM→结构化生成”系统都会遇到的问题。

本质原因是：

# “思考模式”会破坏结构化输出稳定性。

尤其：

* DeepSeek
* Qwen
* Claude
* GPT
* Gemini

只要开启：

```text id="s6w4gc"
reasoning
thinking
chain-of-thought
deep thinking
```

模型就容易：

```text id="rxp38d"
先思考
再解释
最后夹带JSON
```

而不是：

# 纯结构化输出。

---

# 你现在要明确一个核心原则

# “思考”和“结构化输出”必须分离。

这是工业AI系统里极其重要的架构。

---

# 正确架构

不要：

```text id="0ih12e"
一个Prompt
既负责推理
又负责JSON
```

这是错误路线。

---

# 正确做法是：

# 两阶段生成

---

# 第一阶段：Thinking

让模型：

* 推理
* 分析
* 规划
* 状态机设计

输出：

```text id="48k6mn"
自然语言推理结果
```

---

# 第二阶段：Structured Generation

再让模型：

# 只输出JSON。

禁止解释。

---

# 正确流程

```text id="8fjlwm"
用户需求
    ↓
Thinking Agent
    ↓
中间语义计划
    ↓
JSON Generator
    ↓
DSL JSON
```

---

# 你现在的问题

你大概率是：

```text id="48zkgv"
"请思考后输出JSON"
```

模型会：

```text id="1odrd0"
分析...
推理...
最终答案：

{
}
```

甚至：

````text id="0pr3kn"
```json
{
}
````

````

还可能：

- 少括号
- 混入解释
- JSON不闭合

---

# 正确方案（非常重要）

# 一、彻底分离 Thinking 与 JSON

---

# 第一请求

## Thinking Prompt

例如：

```text id="wz2my0"
你是PLC控制规划AI。

请分析以下需求：

“控制双电机顺序启动”

输出：

1. 输入信号
2. 输出信号
3. 状态机
4. 定时器需求
5. 互锁关系

不要输出JSON。
````

---

# 得到：

```text id="l1srbv"
输入：
- StartBtn
- StopBtn

输出：
- Motor1
- Motor2

逻辑：
...
```

---

# 第二请求

把：

* 用户需求
* Thinking结果

一起发给：

# JSON生成器。

---

# 第二请求 Prompt

````text id="xtt11v"
你是PLC DSL生成器。

严格输出JSON。

禁止解释。
禁止Markdown。
禁止```json。
禁止额外文字。

输出必须是合法JSON。

格式：

{
  "variables": [],
  "networks": []
}
````

---

# 这才是工业级方案。

---

# 二、强制“JSON ONLY”

这是重点。

---

# 你现在Prompt里必须有：

```text id="dfnfrl"
STRICT JSON ONLY
```

以及：

````text id="j4ubkw"
Do not explain.
Do not use markdown.
Do not wrap in ```json.
Return valid JSON only.
````

---

# 三、增加 JSON 提取器（必须）

因为：

# 再强的Prompt也不能100%保证。

所以：

你必须：

# 永远做后处理。

---

# 正确做法

---

# 从回复中提取JSON

例如：

```cpp id="0xyx8x"
size_t start = text.find('{');

size_t end = text.rfind('}');

std::string json =
    text.substr(start, end - start + 1);
```

---

# 然后：

```cpp id="bsv55q"
Json::parse(json);
```

---

# 四、增加 Repair Pass（非常关键）

如果：

```text id="vgsy6q"
JSON解析失败
```

不要直接报错。

而是：

# 自动修复。

---

# 修复Prompt

```text id="njlwm1"
以下JSON不合法。

请修复为合法JSON。

禁止解释。

仅输出JSON。

错误内容：

...
```

---

# 这就是你：

```text id="hhfq95"
Repair Engine
```

真正应该做的事情。

---

# 五、最重要的一点

# 不要让“最终生成器”思考太多。

因为：

# 思考越多，结构越不稳定。

---

# 正确策略

---

# Thinking Model

负责：

* 推理
* 状态机
* 规划

允许自由输出。

---

# Generator Model

负责：

* JSON
* DSL
* XML

必须：

```text id="e9mdlf"
deterministic
strict
structured
```

---

# 六、你现在最推荐的方案

对于：

# DeepSeek V4 Flash

建议：

---

# 第一阶段

开启思考。

用于：

```text id="eb3jrr"
需求分析
状态机规划
互锁分析
```

---

# 第二阶段

关闭思考。

低temperature。

专门生成JSON。

---

# 七、参数建议（非常重要）

---

# Thinking阶段

```text id="nkmkzv"
temperature: 0.7
thinking: on
```

---

# JSON阶段

```text id="gjlwmq"
temperature: 0.1
thinking: off
```

---

# 八、你真正需要新增的模块

---

# StructuredOutputEngine

例如：

```cpp id="d0f40n"
class StructuredOutputEngine
{
public:

    std::string GenerateDslJson(
        const std::string& plan);

    std::string RepairJson(
        const std::string& invalidJson);

    bool ValidateJson(
        const std::string& json);
};
```

---



# 十、你现在的最佳实践（总结）

# 正确架构：

```text id="1jlwmu"
用户需求
    ↓
Thinking Agent
    ↓
Semantic Plan
    ↓
JSON Generator
    ↓
JSON Extractor
    ↓
JSON Validator
    ↓
Repair Engine
    ↓
DSL
```

这是：

# 工业级LLM编译流水线。
